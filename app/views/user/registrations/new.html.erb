<div class = "container">

  <div class = "row mt-3 mb-3">

    <h5>Sign up</h5>

  </div>

  <div class = "row mt-3 mb-3">

  <%= form_with model: @user, url: user_registration_path do |f| %>
  <%= render "user/shared/error_messages", resource: resource %>

  <table class = "table  table-sm table-borderless">

    <tr><td><%= f.label :ユーザー名 %></td><td>
    <%= f.text_field :name%></td></tr>

    <tr><td><%= f.label :自己紹介 %></td><td>
    <%= f.text_area :introduction ,rows: 10, cols: 50 %></td></tr>

    <tr><td><%= f.label :プロフィール画像 %></td><td>
    <%= f.file_field :profile_image, accept: "image/*" %></td></tr>

    <tr><td><%= f.label :メールアドレス %></td><td>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %></td></tr>

    <tr><td><%= f.label :パスワード %></td><td>
    <% if @minimum_password_length %>
    <em>（<%= @minimum_password_length %>文字以上）</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>

    <tr><td><%= f.label :パスワード（確認用）%></td><td>
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %></td></tr>

  </table>

    <div class = "row mt-3 mb-3">&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;

      <div class="col">
      <%= f.submit "Sign up",class:"btn btn-success" %>

      <% end %>

      </div>

      <div>既に登録済みの方</div>

      <div><%= render "user/shared/links" %>からログインを行ってください。</div>


    </div>

  </div>

</div>